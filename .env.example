# SmartClaw Docker Environment Variables
# Copy to .env and customize as needed

# Matrix server port (default: 8008)
MATRIX_PORT=8008

# Conduit configuration
CONDUIT_CONFIG=/etc/conduit/conduit.toml

# Optional: Override server name (default: localhost)
# CONDUIT_SERVER_NAME=localhost
